Construct a tree using the trees constructor.  Decide on your goal then call search function.  It will find all available paths and then print them to you. Then it will print the shortest paths. I use space counting on a string to calculate the size of the road but you can use vector<int> instead of string and append using it.
